#include <iostream>
#include "StatsManager.h"

int main()
{
	/*using namespace std;
	StatsManager statMan;

	statMan.AddAttack(false);
	statMan.AddAttack(true);
	statMan.AddDash();
	statMan.AddJump();

	cout << "stats added" << endl;

	if (statMan.Append()) {
		cout << "stats appended" << endl;
	}
	else {
		cout << "couldn't write to file" << endl;
	}

	cout << "eop" << endl;*/
	
}